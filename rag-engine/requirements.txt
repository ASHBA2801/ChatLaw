# RAG Engine — Python dependencies

# --- Core & Ingestion (Phase RAG-01) ---
pymupdf>=1.23.0
pydantic>=2.0.0
python-dotenv>=1.0.0
tqdm>=4.65.0
pytest>=7.0.0

# --- Core API (Future Phases) ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- Database (Future Phases) ---
psycopg[binary,pool]>=3.1
# pgvector           # pgvector client support

# --- Retrieval & Generation (Future Phases) ---
# google-generativeai # Legacy SDK; not used
google-genai         # Gemini embeddings + grounded generation

# --- Text / parsing ---
# pypdf              # PDF parsing for Law_files sources
