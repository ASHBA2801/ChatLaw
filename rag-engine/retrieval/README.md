# Retrieval

Intended to hold retrieval implementations:

- **Vector retrieval** (pgvector cosine similarity)
- **Keyword retrieval** (BM25 / full-text)
- **Exact legal citation retrieval** (e.g. `Section 51, BNS`)
- **Hybrid retrieval** (combining the above)

**Not implemented yet** — manual development pending.