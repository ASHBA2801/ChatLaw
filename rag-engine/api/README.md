# RAG Engine HTTP API

This directory will contain the HTTP API server for the RAG Engine.

The full API contract is documented in `docs/api/`. The web application
communicates with the RAG Engine **exclusively** through this API — it never
embeds RAG logic.

Implementation pending manual development.